{
  "name": "@repo/db",
  "description": "Contains all the database entities",
  "scripts": {
    "build": "rimraf dist && tsup",
    "build:prod": "rimraf dist && tsup"
  },
  "files": [
    "dist"
  ],
  "type": "module",
  "main": "./dist/index.cjs",
  "module": "./dist/index.js",
  "types": "./src/index.ts",
  "devDependencies": {
    "@repo/exchange-info": "workspace:*",
    "@types/node": "^22.10.2",
    "drizzle-kit": "^0.30.1",
    "glob": "^11.0.0",
    "rimraf": "^6.0.1",
    "tsup": "^8.3.5",
    "typescript": "^5.7.2"
  },
  "dependencies": {
    "drizzle-orm": "^0.38.2",
    "drizzle-zod": "^0.6.0",
    "zod": "^3.24.1"
  }
}
