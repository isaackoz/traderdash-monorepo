{
  "name": "@repo/shared-schemas",
  "description": "Shared ZOD schemas for forms, api, etc.",
  "scripts": {
    "build": "rimraf dist && tsup",
    "build:prod": "rimraf dist && tsup"
  },
  "files": [
    "dist"
  ],
  "type": "module",
  "main": "./dist/index.cjs",
  "module": "./dist/index.js",
  "types": "./src/index.ts",
  "devDependencies": {
    "glob": "^11.0.0",
    "rimraf": "^6.0.1",
    "tsup": "^8.3.5",
    "typescript": "^5.7.2"
  },
  "peerDependencies": {
    "@repo/db": "workspace:*",
    "@repo/exchange-info": "workspace:*"
  },
  "dependencies": {
    "zod": "^3.24.1"
  }
}
